### 1. 请求到达与预处理阶段（CPU 侧主导，影响 TTFT）

1. **Prompt 接收与聊天模板应用**  
   把 system prompt + history + user message 按模板拼接，插入 <|im_start|> 等控制 token。  
   （为了角色清晰和边界控制）

2. **Tokenization**  
   用 tokenizer（通常 tiktoken 或 sentencepiece）把文本 → token_ids。  
   （byte-level BPE，兼容任意字符）

3. **长度检查 & 截断 / 滑动窗口**  
   如果总长 > 上下文窗口（如 128k/1M），截断最早历史或保留关键片段。  
   （为了避免 OOM，常见丢弃旧对话）

4. **Padding & attention_mask 生成**（batch 时）  
   right-pad 到 batch 最长长度，生成 0/1 mask。  
   （为了 GPU 并行，padding 位置不参与计算）

### 2. 进入 GPU：Prefill 阶段（提示处理，一次性计算所有输入 token）

5. **Embedding + Positional Encoding**  
   token_ids → embedding lookup + RoPE（旋转位置编码）。  
   （捕捉语义 + 顺序）

6. **Transformer 前向传播（多层）**

   - 多头注意力（self-attention）
   - 计算 Q、K、V
   - 注意力分数 = Q·K^T / √d
   - softmax + 加权 V
   - 但关键：**完整 prompt 一次性算所有 token 的 KV**  
     （这一步是 compute-bound，矩阵-矩阵乘法，GPU 利用率高）

7. **KV Cache 初始化（最关键的内存起点）**  
   把 prompt 所有 token 的 Key 和 Value 存入 KV cache。  
   形状通常： [batch, num_layers, seq_len, num_kv_heads, head_dim]  
   内存占用 ≈ 2 × layers × seq_len × hidden_size × bytes_per_element  
   （例如 Llama-3-70B fp16：~2 bytes × 80 层 × 128k seq ≈ 几十 GB）

8. **生成第一个新 token**  
   对最后一个位置的 hidden state → lm_head → logits → 采样/argmax 得到 token_1。  
   （TTFT = prefill 时间 + 采样时间，通常几百 ms ~ 几秒）

**Prefill 阶段优化手段（2026 主流）**

- **FlashAttention-3 / FlashInfer**：融合 IO-aware attention 内核，减少 HBM 读写，加速 2–4×，内存省 50%+（Dao 系列 + 最新 block-sparse 变体）。
- **Chunked / Disaggregated Prefill**：把超长 prompt 分块计算，或 prefill 和 decode 分离到不同 GPU（减少单卡压力）。
- **连续批处理（Continuous / In-flight Batching）**：允许新请求在 prefill 中途加入 batch（vLLM 核心，吞吐提升 2–10×）。
- **量化（Quantization）**：权重用 INT4/AWQ/GPTQ/FP8，KV cache 用 INT8/FP8，内存减半到 1/4，速度提升 1.5–3×。

### 3. Decode 阶段（自回归生成，逐 token 循环，内存带宽杀手）

9. **准备当前步输入**  
   只取最新生成的 1 个 token → embedding + RoPE（位置 = 当前 seq_len）。

10. **Transformer 前向（只算新 token）**

    - Q 来自新 token，K/V 来自 KV cache + 新 token
    - 注意力 = 只算新 Q 对所有历史 KV 的匹配（矩阵-向量乘法，内存-bound）

11. **KV Cache 追加**  
    把新 token 的 K 和 V append 到对应层的 cache。  
    （每生成一个 token，KV cache 增长 ≈ 2 × hidden_size × bytes）

12. **Logits → 采样下一个 token**  
    lm_head → logits → greedy / top-p / temperature 采样 → token_t+1。

13. **循环步骤 9–12** 直到遇到 EOS 或达到 max_tokens。

**Decode 阶段是推理真正瓶颈**（GPU 算力利用率常 <30%，内存带宽吃满）

**Decode 核心优化手段（2026 主流）**

14. **PagedAttention / RadixAttention**（vLLM 首创，2023–2026 标配）  
    KV cache 不连续分配，而是分页（像 OS 虚拟内存），非连续块用 page table 索引。  
    收益：避免碎片化，内存利用率提升 50–90%，支持 2–24× 并发（vLLM 经典数据）。

15. **连续批处理（Continuous Batching）**  
    每生成一步后，立即把已完成的序列踢出 batch，把新等待的请求插进来。  
    避免“等最长序列”的浪费，吞吐提升 2–10×，p50 延迟大幅下降。

16. **Speculative Decoding（投机解码 / 推测解码）**  
    用小 draft 模型（0.1–1B）一次性猜 N 个 token → 大模型并行验证 → 接受连续正确前缀，拒绝后回滚。  
    主流变体：Medusa、Lookahead、EAGLE、Multi-Token、Batch Speculative。  
    收益：1.5–3× 加速（甚至更高），vLLM/SGLang 2026 已成熟支持。

17. **量化 + 低比特 KV cache**  
    KV cache 常用 INT8/FP8，甚至 4bit（QuIP#、AQLM），内存再省 50%。  
    结合权重量化，整卡可多跑 2–4× 请求。

18. **多 GPU 并行（TP/PP/EP）**

    - Tensor Parallel：一层内分头/分维度
    - Pipeline Parallel：层间切分
    - Expert Parallel（MoE 专用）  
      （TensorRT-LLM 特别擅长自定义内核 + 多卡）

19. **调度与路由（Scheduler）**  
    优先短请求、先 prefill 后 decode、动态调整 batch size、内存阈值 eviction 等。  
    （SGLang 的 RadixAttention + vLLM 的 block manager 是典型）

### 4. 输出后处理与返回

20. **Detokenization**  
    token_ids → 文本，处理特殊 token。

21. **Streaming 返回**（生产必备）  
    边生成边返回，降低感知延迟。
