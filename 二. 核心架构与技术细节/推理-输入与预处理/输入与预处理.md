### 1.1 原始用户输入接收

- 接收多轮对话历史 + 系统提示 + 当前用户消息（原始字符串列表或单字符串）
- （为了拿到最原始的、未经任何格式化的文本输入）

### 1.2 应用聊天模板（Chat Template Application）

- 加载预定义的 chat template（通常是模型家族自带的 jinja2 风格模板）
- 根据角色（system / user / assistant）依次填充内容
- 在适当位置插入特殊控制 token，例如：
  - `<|im_start|>system` / `<|im_end|>`
  - `<|im_start|>user` / `<|im_end|>`
  - `<|im_start|>assistant` / `<|im_end|>`
  - 或 Llama-3 风格的 `<|begin_of_text|>`、`<|eot_id|>` 等
- 拼接成完整的一段带特殊 token 的 prompt 文本
- （为了让模型明确知道“谁在说话”、对话边界在哪里、当前是 assistant 回合；就像给剧本打上角色标签和场景分割线）

### 1.3 Unicode 规范化（Unicode Normalization）

- 对拼接后的完整 prompt 文本执行 NFKC（或 NFD→NFC）规范化
- 处理：全角 → 半角、兼容字符分解与重组、零宽字符移除等
- （为了消除编码变体导致的相同语义不同 token 问题；就像把“見た目”和“見た目”这种视觉相同但码点不同的写法统一）

### 1.4 Pre-tokenization（预分词 / 粗切分）

- 根据语言规则初步切分文本：
  - 英文/多数西方语言：按空格 + 标点 + 数字边界
  - 中文/日文/韩文：通常按字符（character）逐个拆，或使用简单规则（如标点独立）
  - 代码/特殊格式：保留空格、换行、缩进等作为分隔依据
- 得到初步的“word-like” 单元列表
- （为了给后续 BPE 提供合理的初始合并单元；就像先把整篇文章粗略切成“词块”而不是直接按字/字节）

### 1.5 Byte-level 转换（Byte fallback 准备）

- 将每个预分词单元编码成 UTF-8 字节序列
- 把整个 prompt 变成连续的字节流（byte stream）
- （为了让 BPE 能在字节层面工作，支持任意 Unicode、emoji、乱码、控制字符；就像把所有文字都翻译成通用的“字节砖块”）

### 1.6 BPE 合并迭代（核心分词过程）

- 从字节级别开始，按照预训练时学到的合并规则表（merge ranks）逐级合并
- 每次找当前序列里“排名最高（最优先）”的字节对，合并成一个新 token
- 重复直到不能再合并（即遇到了规则表边界或已经是词汇表中的完整词）
- 得到最终的 token 符号序列（string tokens），例如 ["Hello", "▁world", "!", "<|im_end|>"]
- （为了把任意文本高效压缩成有限词汇表中的子词序列；就像反复把最常见的“砖块组合”粘成更大的“乐高零件”）

### 1.7 特殊 token 保留与后处理

- 确保模板中插入的控制 token（如 <|im_start|>, <|endoftext|> 等）不被 BPE 拆分（通常它们在词汇表中是单条目）
- 处理可能的 byte fallback：极少数未覆盖的字节会变成 <0xXX> 形式或直接作为单字节 token
- （为了保证控制信号完整无损；就像保护剧本里的“导演指令”不被拆散）

### 1.8 Token → ID 映射（Vocabulary Lookup）

- 用最终 token 列表去查词汇表（通常是一个 dict 或数组）
- 得到对应的整数序列：token_ids = [128000, 31514, 11, 128009, …]
- （为了把符号变成模型真正能索引的数字；就像给每个乐高零件贴上仓库货架编号）

### 1.9 序列长度检查与截断（Truncation / Windowing）

- 计算当前 token_ids 长度 len(token_ids)
- 如果 len > max_context_length（例如 8192、128k、1M 等）：
  - 策略 A：从最前面截断（最常见，丢弃最早的历史）
  - 策略 B：从中间截断，尽量保留 system prompt + 最近对话
  - 策略 C：滑动窗口，只保留最后 N 个 token
  - 策略 D：智能保留（保留开头 system + 结尾用户消息，中间省略）
- 截断后重新生成 attention_mask（1=有效，0=已丢弃）
- （为了适配模型的上下文窗口；就像行李超重时果断扔掉不重要的旧衣服，优先保住机票和身份证）

### 1.10 Padding 与批处理对齐（Padding & Batching，如果是 batch 推理）

- 如果是单条推理：通常 left-pad 或 right-pad 到 batch 中最长序列长度（现代框架多 right-pad）
- 生成 attention_mask：
  - 1 = 真实 token
  - 0 = padding 位置
- 如果使用 flash attention 或高效内核，还可能生成 position_ids、cu_seqlens 等辅助结构
- （为了让 GPU 并行处理不同长度的序列；就像把不同长度的书塞进同样宽的书架，中间用空白隔板挡住不让视线穿过去）

### 1.11 最终输入张量准备

- token_ids → torch.LongTensor 或 jax.numpy array
- attention_mask → 同形状的 0/1 张量
- （可选）position_ids：如果不用 RoPE 而用绝对位置编码，则生成 0,1,2,… 序列
- 把这些打包成模型可以直接接收的输入字典
- （为了进入 embedding 层前的最后一公里准备；就像把所有零件和说明书装进一个标准托盘，送上流水线）
