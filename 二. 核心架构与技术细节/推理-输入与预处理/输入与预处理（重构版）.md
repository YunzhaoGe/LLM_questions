# 推理输入与预处理（对话 → 模型输入张量）

目标：从“多轮对话的原始字符串”开始，按聊天模板组装 prompt，再进入 tokenizer 的编码流程（预分词规则 + 子词算法，常见为 SentencePiece BPE/Unigram 或 byte-level BPE，可能带 byte fallback），随后完成 token→id 映射、长度策略（截断/滑窗/打包）、padding 或变长对齐（如 `cu_seqlens`），最终产出模型可直接消费的输入张量（如 `input_ids`、`attention_mask`、可选 `position_ids`/`cu_seqlens`）。每一步都回答“做了什么/为什么这么做”，并配一句“就像……”的类比，帮助把输入侧全链路串起来。

---

## 1. 接收原始输入（多轮消息）

1. 收集消息：拿到系统提示（system）+ 历史对话（user/assistant）+ 当前用户消息，形态可能是字符串数组或单字符串（为了保留最原始的语义与边界信息，避免过早丢失“角色/轮次/分隔”；就像先把所有聊天记录原封不动收进档案袋，再开始整理归档）

---

## 2. 应用聊天模板（把对话变成可喂给模型的一段 prompt）

1. 加载模板：选用该模型家族对应的 chat template（常见是 jinja2 风格）并按角色顺序填充（为了让同一套模型在不同产品/框架里都能用统一的“对话包装格式”；就像用同一个表格模板把不同人的信息填进去，字段顺序不乱）
2. 插入控制 token：在轮次边界/角色切换/结束位置插入特殊 token，并把所有片段拼成一个完整 prompt 文本（为了让模型显式知道“谁在说话”“一轮到哪里结束”“现在轮到 assistant 输出”；就像剧本里用场记板写清楚角色与分镜切换）：
   - 例：`<|im_start|>system` / `<|im_end|>`、`<|im_start|>user` / `<|im_end|>`、`<|im_start|>assistant` / `<|im_end|>`
   - 或：`<|begin_of_text|>`、`<|eot_id|>` 等风格的边界 token

---

## 3. 文本规范化（可选：把“看起来一样但编码不同”的情况收敛）

1. Unicode normalization（可选）：对拼好的 prompt 做 NFKC（或 NFD→NFC）等规范化，处理全角/半角、兼容字符、零宽字符等（为了降低同一语义因编码差异导致分词不同、进而影响模型行为的概率；但不少线上系统默认不做或仅做很保守的清洗，以免破坏代码、标点与不可见字符的“有意用法”；就像统一排版能减少误会，但过度“纠错”也可能把源码里的空格缩进改坏）

---

## 4. 预分词（Pre-tokenization：tokenizer 内部的“切分规则”）

1. 粗切分单元：由 tokenizer 的预分词规则把文本切成初始单元（不一定按“语言学词”切，有时会刻意保留空格/换行/标点）（为了让后续子词算法在更稳定的边界上工作，并复现训练期的切分习惯；就像先按编辑部的排版规范把稿子分栏，后续装订才不会错位）：
   - 英文等：空格 + 标点 + 数字边界
   - 中/日/韩：常按字符或简化规则（标点独立等）
   - 代码/结构化文本：保留空格、换行、缩进等格式线索

---

## 5. 字节级表示（可选：Byte-level / byte fallback）

1. UTF-8 字节化（可选）：在 byte-level BPE 体系里会把文本映射到字节序列；在 SentencePiece 等体系里也常配 byte fallback，保证任何输入都能落到可编码的单元（为了覆盖任意 Unicode、emoji、罕见字符、乱码与控制字符；就像遇到生僻字时至少能按“编码拼音”读出来，不至于卡死）

---

## 6. 子词编码（把文本压缩成“更大、更常见”的 token）

1. BPE / Unigram：BPE 会按 merge ranks 迭代合并；Unigram 会在词表候选中寻找概率最优分解（两者都是把长字符串变成更短的子词序列）（为了在覆盖率、序列长度与统计稳定性之间取平衡，并严格对齐训练时的 tokenizer；就像把文章拆成出版社认可的“标准词块”，排版与检索都更稳定）
2. 得到 token 序列：输出 string token 列表，例如 `["Hello", "▁world", "!", "<|im_end|>"]`（为了把输入变成模型可识别的离散符号序列；就像把连续文字拆成一串可编号的零件清单）

---

## 7. 特殊 token 与回退（保证控制信号不被破坏）

1. 保留控制 token：确保模板插入的特殊 token 在词表中是单条目，不被 BPE 拆分（为了让“边界/角色/结束”等控制信号语义稳定；就像把“红灯停”的交通标志固定成一个完整图标，不能拆成几块颜色）
2. Byte fallback：对极少数未覆盖字节进行回退编码（可能表现为 `<0xXX>` 或单字节 token）（为了保证任何输入都能被编码成 token 序列而不会失败；就像遇到生僻字时先用拼音/编码占位，至少能继续读下去）

---

## 8. Token → ID（把符号变成可查表的整数索引）

1. 词表查找：用 token 列表查 vocabulary，得到 `token_ids = [id0, id1, ...]`（为了让后续 embedding 能以 O(1) 查表，把离散符号映射到向量；就像把每个零件换成仓库货架编号，机器人拿货才快）

---

## 9. 长度策略（上下文窗口约束下的截断/滑窗/打包）

1. 长度检查：计算 `len(token_ids)` 并与 `max_context_length` 比较（为了保证输入落在模型可处理的上下文窗口内；就像登机箱要先过尺寸框，超了就上不了飞机）
2. 截断/保留策略：当超长时选择一种策略（为了在“可处理长度”与“信息保留”之间做权衡；就像行李超重时优先保留证件和必需品）：
   - 从前截断：丢弃最早历史（最常见）
   - 中间截断：尽量保留 system + 最近对话
   - 滑动窗口：只保留最后 N 个 token
   - 智能保留：保留开头关键段 + 结尾最新消息，中间省略
3. 输入形态：多数实现会直接删除超长 token（而不是“保留原长再用 mask 置零”），`attention_mask` 主要用于 padding；若采用 packed sequence/变长 FlashAttention，则用 `cu_seqlens`/offset 等表达“每段序列的边界”（为了让模型只看见真实内容且算得快；就像把超出的旧纸直接裁掉，而不是把整本书留着再贴一堆“这页作废”的便利贴）

---

## 10. Padding 与 batch 对齐（让 GPU 能并行算不同长度的序列）

1. 对齐长度：在 batch 场景要么 padding 到同一长度，要么用变长表示（现代推理服务常优先变长/packing；若必须 padding，生成阶段常见 left-pad，训练/预填充常见 right-pad，取决于框架与 kernel）（为了把变长序列喂给并行计算内核，同时减少无效算力；就像能按“每本书的实际厚度”打包就别塞一堆泡沫，实在要统一箱子尺寸才用垫片）
2. attention_mask：生成与 `input_ids` 同形状的 0/1 掩码（`1=真实 token，0=padding`）（为了让注意力与损失不受 padding 影响；就像用隔板把空位挡住，告诉系统“这里不是内容”）
3. 额外结构（实现相关）：高效注意力/融合内核可能还需要 `position_ids`、`cu_seqlens` 等辅助信息（为了让底层 kernel 能在不规则长度上高效索引与调度；就像给流水线多发一张工位清单，工人不用自己数步数）

---

## 11. 打包成模型输入（最后一公里：变成张量字典）

1. 构造张量：把 `token_ids` 转为 `torch.LongTensor` / `jax.numpy` array 等，把 `attention_mask` 转为同形状张量（为了进入 embedding/attention 计算时数据类型与形状都正确；就像把零件装进标准托盘并贴好条码，机器才能直接抓取）
2. 可选 position_ids：若使用绝对位置编码（而不是 RoPE 等在注意力内部处理位置），则生成 `position_ids = [0,1,2,...]`（为了让模型区分相同 token 出现在不同位置；就像给每个座位一个座位号，坐哪一排很关键）
3. 输入字典：把上述字段打包为模型前向所需的输入结构（例如 `{"input_ids": ..., "attention_mask": ..., "position_ids": ...}`）（为了把推理入口统一成稳定接口，便于服务化与工程优化；就像把所有材料按清单装箱并封条，交给生产线即可开工）
